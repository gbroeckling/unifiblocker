{
  "config": {
    "step": {
      "user": {
        "title": "Connect to UniFi Controller",
        "description": "Enter your UniFi OS controller details (UCG Max, UDM, etc.).",
        "data": {
          "host": "Host (IP or hostname)",
          "username": "Username",
          "password": "Password",
          "site": "Site ID",
          "verify_ssl": "Verify SSL certificate",
          "scan_interval": "Scan interval (seconds)"
        }
      }
    },
    "error": {
      "invalid_auth": "Invalid username or password.",
      "cannot_connect": "Cannot connect to the UniFi controller.",
      "unknown": "An unexpected error occurred."
    },
    "abort": {
      "already_configured": "UniFi Blocker is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "UniFi Blocker Settings",
        "data": {
          "scan_interval": "Scan interval (seconds)",
          "verify_ssl": "Verify SSL certificate",
          "local_subnet": "Local-only subnet (e.g. 192.168.2)",
          "local_cidr": "Local-only CIDR (e.g. 192.168.2.0/24)",
          "auto_scan_count": "Devices to auto-scan per poll cycle"
        }
      }
    }
  }
}
